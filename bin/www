const fs = require('fs');
const app = require('../app');
const https = require('https');

/* https.createServer({
    cert: fs.readFileSync('certificado-servidor.pem'),
    key: fs.readFileSync('claveprivada.pem')
}, app).listen(app.get('port'), ()=>{
    console.log('Server on port ', app.get('port'));
}); */

app.listen(app.get('port'), ()=>{
    console.log('Server on port ', app.get('port'));
})


